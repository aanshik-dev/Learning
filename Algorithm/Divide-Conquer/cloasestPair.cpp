#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;

pair<pair<int, int>, pair<int, int>> closestPair(vector<pair<int, int>> vect) {
  int mid = vect.size() / 2;
}

int main() {
  vector<pair<int, int>> vect = {{1, 2}, {3, 4}, {5, 6}, {7, 8}};
  sort(vect.begin(), vect.end());

  return 0;
}