#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;

vector<vector<pair<int, int>>> groupInterval(vector<pair<int, int>> &gaps) {
  sort(gaps.begin(), gaps.end());
  vector<vector<pair<int, int>>> res;
}

int main() {
  vector<pair<int, int>> intervals = {{1, 3}, {2, 4}, {3, 5}, {7, 8}};
  return 0;
}